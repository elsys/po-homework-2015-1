#include <stdio.h>

int main()
{
int s[100], r[200], i, a = 0;
int main()
{
int c, r, n, t;
int arr[200];
  while(n%2 == 0 || n > 200)
  {
    scanf("%d", &n);
  }
  for(i = 0; i < 100; i++)
    {
      s[i] = 0;
    }
}while(n <= 0 || n >= 200 || n % 2 == 0);
  for(i = 0; i < n; i++){
    while(e[i] < 1 || e[i] > 100 || s[e[i]] > 1);
      {
        scanf("%d", &e[i]);
      }
  s[e[i]]++;
  }
  for(i = 0; i < 100; i++){
    if([i] == 1)
    if(a == 0)
    a = i;
    c = 0;
    scanf("%d", &arr[i]);
      for(t = 0; t < i; t++){
        if(arr[i] == arr[t])
        c++;
      }
  }while(arr[i] <= 0 || arr[i] > 100 || c > 2);
}
  for(i = 0; i < n; i++){
    if(e[i] == a){
    printf("%d", i);
    return 0;
      for(t = i; t < n;){
        t++;
        if((arr[i] == arr[t]) && (arr[t] != 0)){
          arr[i] = 0;
          arr[t] = 0;
        }
        else if(arr[i] != 0)
        r = arr[i];
      }
}
  return -1;
  printf("%d",r);
  return 0;
}
